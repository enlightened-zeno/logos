[package]
name = "logos-host-tests"
version = "0.1.0"
edition = "2021"

# Standalone workspace — does NOT inherit from the kernel workspace.
# This ensures we compile against the host target, not the bare-metal kernel target.
[workspace]

[dev-dependencies]
