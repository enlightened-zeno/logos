[workspace]
members = [
    "kernel",
    "tests/host",
]
resolver = "2"

[profile.dev]
panic = "abort"
overflow-checks = true

[profile.release]
panic = "abort"
lto = true
opt-level = 2
overflow-checks = true
